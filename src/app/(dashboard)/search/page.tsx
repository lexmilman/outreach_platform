import { ComingSoon } from "@/components/features/coming-soon";

export default function AiSearchPage() {
  return (
    <ComingSoon
      title="AI Search"
      description="Natural-language search across the global DB — compiled to SQL via an LLM. Sprint 5+."
      sprint={5}
    />
  );
}
