import { ComingSoon } from "@/components/features/coming-soon";

export default function AudiencesPage() {
  return (
    <ComingSoon
      title="Audiences"
      description="Imported LinkedHelper cohorts, one per CSV drop. Ships with Sprint 2."
      sprint={2}
    />
  );
}
