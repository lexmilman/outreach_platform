import { ComingSoon } from "@/components/features/coming-soon";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      description="Daily Instantly sync + per-client, per-campaign KPIs, cost breakdown. Ships with Sprint 4."
      sprint={4}
    />
  );
}
