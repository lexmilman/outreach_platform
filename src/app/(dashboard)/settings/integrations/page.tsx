import { ComingSoon } from "@/components/features/coming-soon";

export default function SettingsIntegrationsPage() {
  return (
    <ComingSoon
      title="Integrations"
      description="Vault-backed API key storage per provider (Apify, FindyMail, SignalHire, Instantly, Perplexity, LLMs)."
      sprint={3}
    />
  );
}
