import { expect, test } from "@playwright/test";

test.describe("CSV import", () => {
  test.fixme("uploads LinkedHelper sample, auto-maps columns, inserts rows", async () => {
    // Sprint 2: implement against seeded test Supabase.
  });
});
