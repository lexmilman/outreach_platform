import { test } from "@playwright/test";

test.describe("Enrichment run", () => {
  test.fixme("kicks off a test run on 10 leads, shows diff viewer", async () => {
    // Sprint 3: implement against seeded test Supabase + mocked Apify/FindyMail/SignalHire.
  });
});
