-- Seed local dev data. Run after `supabase db reset`.
-- Assumes you have already created a user via the Auth UI (`supabase auth`).

-- insert into public.organizations (id, name) values
--   ('11111111-1111-1111-1111-111111111111', 'Demo Agency');
-- insert into public.memberships (user_id, org_id, role) values
--   ('<your-auth-user-id>', '11111111-1111-1111-1111-111111111111', 'owner');
-- insert into public.clients (org_id, name, slug, icp_description) values
--   ('11111111-1111-1111-1111-111111111111', 'Acme Corp', 'acme', 'Mid-market SaaS companies in North America');
